{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run main.go",
      "type": "go",
      "request": "launch",
      "mode": "debug",
      "program": "${workspaceFolder}/main.go",
      "cwd": "${workspaceFolder}"
    }
  ]
}